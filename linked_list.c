#include "linked_list.h"


/**
* Single Linked List
* -
*    - Each element is linked by a single pointer
*    - Each element consists of a data member and a pointer
*    - The element at the start of the list is refered as the HEAD
*    - The element at the end of the list is refered as the TAIL
*    - The *next* pointer of the last element is set to NULL
*/

